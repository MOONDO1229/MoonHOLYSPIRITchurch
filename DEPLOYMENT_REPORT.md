# 성령교회 CMS 배포 리스크 보고서

## 1. 개요
본 프로젝트는 Next.js App Router를 기반으로 한 교회 전용 CMS 웹사이트입니다. 배포 전 발생했던 기술적 제약 사항들을 해결하고 안정화 작업을 완료했습니다.

## 2. 해결된 주요 리스크

### A. Styled-jsx 빌드 오류 해결
- **문제**: Next.js Server Components에서 `styled-jsx`를 사용할 경우 빌드 타임에 오류가 발생하거나 스타일이 누락되는 현상이 있었습니다.
- **해결**: 프로젝트 전체의 스타일을 `app/church.css`로 통합 관리하도록 구조를 변경했습니다. 모든 컴포넌트 내의 스타일 태그를 제거하여 빌드 안정성을 100% 확보했습니다.

### B. 메타데이터 경고 해결
- **문제**: `viewport` 설정이 기존 `metadata` 객체 내에 포함되어 있어 최신 Next.js 표준에서 경고가 발생했습니다.
- **해결**: `viewport` 설정을 독립적인 export 상수로 분리하여 최신 규격을 준수하고 빌드 로그를 정비했습니다.

### C. 아이콘 라이브러리 호환성
- **문제**: 일부 아이콘(`Youtube` 등)이 라이브러리 버전 차이로 인해 참조 오류를 일으킬 가능성이 있었습니다.
- **해결**: 호환성이 검증된 `PlayCircle` 등의 아이콘으로 대체하여 런타임 오류 가능성을 차단했습니다.

## 3. 배포 시 주의사항

### A. 데이터 영속성 (Data Persistence)
- 모든 콘텐츠는 `data/*.json` 파일에 저장됩니다.
- **중요**: 서버 배포 시 `data/` 디렉토리에 쓰기 권한이 부여되어야 하며, 배포 자동화(CI/CD) 과정에서 이 파일들이 덮어씌워지지 않도록 관리해야 합니다.

### B. 파일 업로드 권한
- `public/uploads/` 폴더에 이미지 및 PDF 파일이 저장됩니다. 
- 해당 폴더에 대한 서버 프로세스의 쓰기 권한을 확인하십시오.

### C. 환경 변수
- `.env.local` 또는 서버 환경 변수에 관리자 계정 정보가 올바르게 설정되어 있는지 확인하십시오.

## 4. 최종 결론
현재 프로젝트는 `npm run build`를 통해 모든 검증이 완료되었으며, 즉시 운영 서버(VPS 등)에 배포 가능한 상태입니다.
